# Contextual Panels Need Dual Identity And Counted Results

When a contextual panel operates on a later item through a template owner, render both identities rather than replacing the active item with the owner. Batch feedback should pair each response-ordered folder list with its count, including failures, so operators can scan scope and outcomes together.
